{{- $remote := or (env "CODESPACES" | not | not) (env "SSH_CONNECTION" | not | not) (env "KUBERNETES_SERVICE_HOST" | not | not) (env "container" | not | not) (env "REMOTE_CONTAINERS" | not | not) (stat "/.dockerenv" | not | not) -}}

[tools]
# Shell essentials (always available)
fzf = "latest"
ripgrep = "latest"
fd = "latest"
bat = "latest"
jq = "latest"
eza = "latest"

# Editor & dev tools
neovim = "latest"
lazygit = "latest"
starship = "latest"

# Utilities
dust = "latest"
gum = "latest"
tmux = "latest"
lsd = "latest"
{{- if eq .chezmoi.os "linux" }}
btop = "latest"
{{- end }}

# Runtime
bun = "latest"
rust = "latest"

{{- if not $remote }}
# Local only (not needed in containers/codespaces)
chezmoi = "latest"
github-cli = "latest"

# Cloud CLIs
awscli = "latest"
azure-cli = "latest"

# Python tooling
python = "latest"
uv = "latest"

# Kubernetes core (needed everywhere locally)
omnictl = "latest"
flux2 = "latest"
{{- end }}
